#include<stdio.h>
main()
{
int a[50][50]; 
int max[50][50];
int min[50][50];
int n, m, i, j,t,c,res;
res=0;
printf("nombre de lignes (max.50) : ");
scanf("%d", &n );
printf("nombre de colonnes (max.50) : ");
scanf("%d", &m );
for (i=0; i<n; i++)
for (j=0; j<m; j++)
{
printf("Elément[%d][%d] : ",i,j);
scanf("%d", &a[i][j]);
}

printf("matrice donnée :\n");
for (i=0; i<n; i++)
{
for (j=0; j<m; j++)
printf("%7d", a[i][j]);
printf("\n");
}
for (i=0; i<n; i++)
{
for (j=0; j<n; j++){
for(t=0;t<n;t++){
	if(a[i][j]>a[i][t]){
		res=1;
	}
}
for(t=0;t<m;t++){
	if(a[i][j]<=a[t][j]){
		res=1;
	}
}
}
if(res==0){
	printf("les point-cols dans le tableau est %d",a[i][j]);
	c++;
}
}
if (c==0)
printf("Le tableau ne contient pas de points-cols.\n");
return 0;
}
